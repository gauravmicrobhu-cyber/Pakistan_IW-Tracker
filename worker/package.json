{
  "name": "prism-iw-tracker-submit-worker",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "echo 'No build step needed — this Worker ships plain ES modules, wrangler deploy handles bundling.'",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
